apiVersion: v1
kind: Secret
metadata:
  name: dclaw-assets-secret
  namespace: dclaw-core
  labels:
    app.kubernetes.io/name: dclaw-assets-backend
    app.kubernetes.io/part-of: dclaw-platform
type: Opaque
stringData:
  DATABASE_URL: "postgresql+asyncpg://postgres:postgres@dclaw-shared-postgres:5432/dclaw_assets"
  APP_NAME: "DClaw Assets"
