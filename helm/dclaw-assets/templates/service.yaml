apiVersion: v1
kind: Service
metadata:
  name: {{ include "dclaw-assets.name" . }}
  labels:
    app.kubernetes.io/name: {{ include "dclaw-assets.name" . }}
spec:
  type: {{ .Values.frontend.service.type }}
  ports:
    - port: {{ .Values.frontend.service.port }}
      targetPort: 3043
      name: frontend
    - port: {{ .Values.backend.service.port }}
      targetPort: 8043
      name: backend
  selector:
    app.kubernetes.io/name: {{ include "dclaw-assets.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
